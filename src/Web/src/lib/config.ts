// Check environment variables for API base URL
// Fallback to Vite environment variable if not set
// Fallback to localhost if not set

// console.log('API URL from environment variable VITE_API_URL:', import.meta.env.VITE_API_URL);
export const API_BASE_URL = '/api';